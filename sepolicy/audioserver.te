allow audioserver cust_file:dir { search };

allow audioserver rootfs:lnk_file { getattr };

allow audioserver binder_per_mgr_service:service_manager find;

allow audioserver socket_device:file write;

allow audioserver device:file { read write open };
